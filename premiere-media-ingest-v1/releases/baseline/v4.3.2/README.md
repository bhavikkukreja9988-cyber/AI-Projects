# Media Ingest V4.3.2 — Known-good baseline

V4.3.2 is the established user-confirmed working baseline for the core Media Ingest workflow.

The baseline is intentionally kept separate from active releases so it is easy to find as a fallback without looking like the current development version.

The original historical implementation remains available through Git history if the exact source needs to be reconstructed later.
